apiVersion: v1
kind: Service
metadata:
  name: rustfs
  namespace: object-storage
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  selector:
    app: rustfs
  ports:
  - name: s3-api
    port: 9000
    targetPort: 9000
  type: ClusterIP
